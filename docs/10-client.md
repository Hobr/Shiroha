# 客户端交互

> 文档定位：本文只定义管理面入口与能力范围，不展开具体命令语法。若后续需要稳定 CLI/API 契约，应另立专门文档。

当前管理入口名称为 `sctl`，通过 Controller 管理接口交互。

初期范围：

- 部署 / 卸载 WASM 模块
- 创建 / 查询 / 控制状态机实例
- 查询 task / 执行记录
- 查看节点状态

非目标：

- 直接承载业务执行逻辑
- 绕过 Controller 管理接口直连内部模块

未来计划：TUI / Web。
