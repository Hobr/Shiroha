# Shiroha Development Guide

> 本地开发环境、常用命令与校验流程

## 当前阶段

仓库当前以设计文档和 workspace 骨架为主。开发工作的重点是：

- 收敛模块边界
- 明确运行时与接口契约
- 逐步把设计文档中的约束落到代码

如果某次变更修改了架构、接口或执行语义，请在同一提交中同步更新 `docs/`。

## 开发约定

- 新代码放到职责最小且边界稳定的 crate 中
- 避免新增 `common`、`utils` 这类聚合式模块
- 架构或接口变化必须同步更新 `docs/`
- 为核心执行逻辑优先补充重放、恢复和故障语义相关测试
