# 能力与权限系统

## 声明

WASM 模块在状态机定义中声明依赖的能力，区分 **必需** 和 **可选**。

## 校验流程

宿主维护能力策略（允许/拒绝列表），加载 WASM 时：

1. 提取能力清单
2. 必需能力未授权 → 拒绝加载
3. 可选能力未授权 → 链接为不可用桩，调用时返回错误而非崩溃
4. 只链接已授权能力对应的 Host 函数

## 扩展方式

新增能力只需：添加 WIT interface → SDK 封装 → wasm-engine 注册链接器。不影响现有模块。
