# v0.1 Acceptance Evidence

This file maps the v0.1 PRD acceptance criteria to executable evidence. Test
names are stable search keys; line numbers are intentionally omitted.

| Criterion | Evidence |
|---|---|
| AC1 | `just validate-example`; `definition_load_prepares_host_ir_with_baseline_wasi` |
| AC2 | `validation_aggregates_structural_issues`; `import_policy_accepts_only_linked_wasi_preview2_interfaces`; `unsupported_wasi_and_non_wasi_imports_are_rejected_before_linking`; `incompatible_component_shape_fails_during_preparation` |
| AC3 | `engine_uses_fixed_lifecycle_order_and_staged_context`; `self_transition_exits_and_reenters_the_same_state`; `startup_entry_fault_never_creates_a_committed_instance` |
| AC4 | `engine_evaluates_guards_in_declaration_order`; `failure_target_commits_failure_effects_then_entry`; `business_failure_without_target_discards_effects_and_fails`; `logical_timeout_selects_its_explicit_transition`; `engine_commits_unhandled_cancel_as_cancelled`; `guest_declared_error_preserves_code_and_payload` |
| AC5 | `run_to_completion_drains_internal_events_fifo`; `run_to_completion_enforces_microstep_limit`; `engine_reports_unhandled_event_without_mutation` |
| AC6 | `atomic_commit_discards_staged_changes_after_runtime_fault`; `business_failure_without_target_discards_effects_and_fails` |
| AC7 | `recreate_executor_restores_from_host_snapshot`; `restore_rejects_a_snapshot_from_another_machine`; `restore_rejects_oversized_lifecycle_payloads`; `local_component_recreates_guest_from_host_snapshot` |
| AC8 | `payload_envelope_round_trips_without_interpretation`; `payload_helper_uses_the_v0_1_json_profile` |
| AC9 | `fuel_limit_stops_an_infinite_guest_action`; `epoch_deadline_stops_an_infinite_guest_action`; `memory_limit_stops_guest_linear_memory_growth`; `memory_limit_is_aggregated_across_store_memories`; `oversized_input_is_a_structured_resource_fault`; `oversized_payload_metadata_is_a_structured_resource_fault`; `oversized_business_failure_payload_is_not_committed`; `oversized_guest_fault_payload_is_replaced_by_a_limit_fault`; `run_to_completion_enforces_microstep_limit` |
| AC10 | `adapter_contract_has_no_wasmtime_types`; `cargo tree -p shiroha-core` |
| AC11 | `local_component_runs_through_the_public_facade`; exclusive `&mut self` on `LocalMachine::dispatch` prevents safe Rust reentrant dispatch |
| AC12 | `core_engine` and `wasm_runtime` Criterion suites; [`../benchmarks/v0.1-baseline.md`](../benchmarks/v0.1-baseline.md); named `shiroha.*` tracing spans |
| AC13 | Root `README.md`, especially v0.1 scope, baseline WASI, compatibility, and roadmap sections |

The full release check is the Phase 8 command set in the active Trellis
implementation plan. Generated `target/` outputs and Criterion result data are
not committed.
