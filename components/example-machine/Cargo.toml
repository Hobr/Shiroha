[package]
name = "example-machine"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.0"
license = "GPL-3.0-only"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
shiroha-guest = { path = "../../crates/shiroha-guest" }

[workspace]
